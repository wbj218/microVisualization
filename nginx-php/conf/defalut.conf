upstream fastcgi_backend {
        server unix:/var/run/php-fpm.sock;
        # server unix:/php7/unix/php-fpm.sock;
        keepalive 32;
}

server {
        listen   80; ## listen for ipv4; this line is default and implied
        listen   [::]:80 default ipv6only=on; ## listen for ipv6

        root /var/www/html;
        # root /nginx/html;
        #index index.php index.html index.htm;

        # Make site accessible from http://localhost/
        server_name _;

        # Disable sendfile as per https://docs.vagrantup.com/v2/synced-folders/virtualbox.html
        sendfile on;
        add_header X-Request-ID $request_id;

        # Add stdout logging
        #error_log /dev/stdout info;
        #access_log /dev/stdout;

        # Add option for x-forward-for (real ip when behind elb)
        #real_ip_header X-Forwarded-For;
        #set_real_ip_from 172.16.0.0/12;

        location / {
                # First attempt to serve request as file, then
                # as directory, then fall back to index.html
                try_files $uri $uri/ =404;
        }

        error_page 404 /404.html;
        location = /404.html {
                root /var/www/errors;
                internal;
        }

        location ^~ /ngd-style.css {
            alias /var/www/errors/style.css;
            access_log off;
        }

        location ^~ /ngd-sad.svg {
            alias /var/www/errors/sad.svg;
            access_log off;
        }

        # pass the PHP scripts to FastCGI server listening on socket
        #


        location ~* /GetPage.php$ {
                try_files $uri =404;
                fastcgi_split_path_info ^(.+\.php)(/.+)$;
                fastcgi_keep_conn on;
                fastcgi_buffer_size 1024k;
                fastcgi_buffers 64 1024k;
                fastcgi_busy_buffers_size 1024k;
                fastcgi_pass unix:/var/run/php-fpm.sock;
                # fastcgi_pass unix:/php7/unix/php-fpm.sock;
                fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
                fastcgi_param SCRIPT_NAME $fastcgi_script_name;
                fastcgi_param X-Request-ID $request_id;
                fastcgi_index GetPage.php;
                include fastcgi_params;
        }

        location ~* /ComposeReview.php$ {
                try_files $uri =404;
                fastcgi_split_path_info ^(.+\.php)(/.+)$;
                fastcgi_keep_conn on;
                fastcgi_buffer_size 1024k;
                fastcgi_buffers 64 1024k;
                fastcgi_busy_buffers_size 1024k;
                fastcgi_pass unix:/var/run/php-fpm.sock;
                # fastcgi_pass unix:/php7/unix/php-fpm.sock;
                fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
                fastcgi_param SCRIPT_NAME $fastcgi_script_name;
                fastcgi_param X-Request-ID $request_id;
                fastcgi_index ComposeReview.php;
                include fastcgi_params;
        }

        location ~ /hls/(?<user>.+)/(?<req_id>.+)/(?<video>.+) {
                auth_request    /auth;
                proxy_pass      http://128.253.128.64:32790/hls/$video;
                fastcgi_param X-Request-ID $request_id;
        }

        location = /auth {
                rewrite ^/.* /auth.php?key=$request_uri;
                fastcgi_param X-Request-ID $request_id;
        }

        location ~* /auth.php {
                try_files $uri = 404;
                fastcgi_split_path_info ^(.+\.php)(/.+)$;
                fastcgi_keep_conn on;
                fastcgi_buffer_size 1024k;
                fastcgi_buffers 64 1024k;
                fastcgi_busy_buffers_size 1024k;
                fastcgi_pass unix:/var/run/php-fpm.sock;
                fastcgi_param X-Request-ID $request_id;
                # fastcgi_pass unix:/php7/unix/php-fpm.sock;
                fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
                fastcgi_param SCRIPT_NAME $fastcgi_script_name;
                fastcgi_index auth.php;
                include fastcgi_params;
        }


        location ~* \.(jpg|jpeg|gif|png|css|js|ico|xml)$ {
                expires           5d;
        }

        # deny access to . files, for security
        #
        location ~ /\. {
                log_not_found off;
                deny all;
        }
        location ^~ /.well-known {
                allow all;
        }
}