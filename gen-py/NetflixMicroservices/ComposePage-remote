#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.10.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
if sys.version_info[0] > 2:
    from urllib.parse import urlparse
else:
    from urlparse import urlparse
from thrift.transport import TTransport, TSocket, TSSLSocket, THttpClient
from thrift.protocol.TBinaryProtocol import TBinaryProtocol

from NetflixMicroservices import ComposePage
from NetflixMicroservices.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
    print('')
    print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] [-novalidate] [-ca_certs certs] [-keyfile keyfile] [-certfile certfile] function [arg1 [arg2...]]')
    print('')
    print('Functions:')
    print('  void ping()')
    print('  MoviePage compose_page(string req_id, string movie_id, string user_id)')
    print('  void upload_plot(string req_id, string movie_id, string plot)')
    print('  void upload_rating(string req_id, string movie_id, string rating)')
    print('  void upload_thumbnail(string req_id, string movie_id, string thumbnail)')
    print('  void upload_cast_info(string req_id, string movie_id,  cast_info)')
    print('  void upload_photo(string req_id, string movie_id, string photo)')
    print('  void upload_video(string req_id, string movie_id, string video)')
    print('  void upload_movie_review(string req_id, string movie_id,  reviews)')
    print('  void upload_watch_next(string req_id, string user_id,  watch_next)')
    print('')
    sys.exit(0)

pp = pprint.PrettyPrinter(indent=2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
validate = True
ca_certs = None
keyfile = None
certfile = None
http = False
argi = 1

if sys.argv[argi] == '-h':
    parts = sys.argv[argi + 1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    argi += 2

if sys.argv[argi] == '-u':
    url = urlparse(sys.argv[argi + 1])
    parts = url[1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    else:
        port = 80
    uri = url[2]
    if url[4]:
        uri += '?%s' % url[4]
    http = True
    argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
    framed = True
    argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
    ssl = True
    argi += 1

if sys.argv[argi] == '-novalidate':
    validate = False
    argi += 1

if sys.argv[argi] == '-ca_certs':
    ca_certs = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-keyfile':
    keyfile = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-certfile':
    certfile = sys.argv[argi+1]
    argi += 2

cmd = sys.argv[argi]
args = sys.argv[argi + 1:]

if http:
    transport = THttpClient.THttpClient(host, port, uri)
else:
    if ssl:
        socket = TSSLSocket.TSSLSocket(host, port, validate=validate, ca_certs=ca_certs, keyfile=keyfile, certfile=certfile)
    else:
        socket = TSocket.TSocket(host, port)
    if framed:
        transport = TTransport.TFramedTransport(socket)
    else:
        transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol(transport)
client = ComposePage.Client(protocol)
transport.open()

if cmd == 'ping':
    if len(args) != 0:
        print('ping requires 0 args')
        sys.exit(1)
    pp.pprint(client.ping())

elif cmd == 'compose_page':
    if len(args) != 3:
        print('compose_page requires 3 args')
        sys.exit(1)
    pp.pprint(client.compose_page(args[0], args[1], args[2],))

elif cmd == 'upload_plot':
    if len(args) != 3:
        print('upload_plot requires 3 args')
        sys.exit(1)
    pp.pprint(client.upload_plot(args[0], args[1], args[2],))

elif cmd == 'upload_rating':
    if len(args) != 3:
        print('upload_rating requires 3 args')
        sys.exit(1)
    pp.pprint(client.upload_rating(args[0], args[1], args[2],))

elif cmd == 'upload_thumbnail':
    if len(args) != 3:
        print('upload_thumbnail requires 3 args')
        sys.exit(1)
    pp.pprint(client.upload_thumbnail(args[0], args[1], args[2],))

elif cmd == 'upload_cast_info':
    if len(args) != 3:
        print('upload_cast_info requires 3 args')
        sys.exit(1)
    pp.pprint(client.upload_cast_info(args[0], args[1], eval(args[2]),))

elif cmd == 'upload_photo':
    if len(args) != 3:
        print('upload_photo requires 3 args')
        sys.exit(1)
    pp.pprint(client.upload_photo(args[0], args[1], args[2],))

elif cmd == 'upload_video':
    if len(args) != 3:
        print('upload_video requires 3 args')
        sys.exit(1)
    pp.pprint(client.upload_video(args[0], args[1], args[2],))

elif cmd == 'upload_movie_review':
    if len(args) != 3:
        print('upload_movie_review requires 3 args')
        sys.exit(1)
    pp.pprint(client.upload_movie_review(args[0], args[1], eval(args[2]),))

elif cmd == 'upload_watch_next':
    if len(args) != 3:
        print('upload_watch_next requires 3 args')
        sys.exit(1)
    pp.pprint(client.upload_watch_next(args[0], args[1], eval(args[2]),))

else:
    print('Unrecognized method %s' % cmd)
    sys.exit(1)

transport.close()
